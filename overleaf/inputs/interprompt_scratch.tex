\begin{lstlisting}[backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2,
    language=Python, 
    caption={Inter-prompt stability scratch function},
    label=lst:inter-scratch]
Function interprompt_stochasticity(original_text, prompt_postfix, nr_variations, temperatures, iterations, bootstrap_samples, print_prompts, edit_prompts_path, plot, save_path, save_csv):

    # Dictionary for KA scores
    ka_scores = {}

    # List for all annotated data
    all_annotated = []

    # Generate paraphrases and annotate data for each temperature
    For each temperature in temperatures:
        paraphrases = generate_paraphrases(original_text, prompt_postfix, 
                                           nr_variations, temperature)
        annotated = []
        For each iteration i in iterations:
            For each paraphrase in paraphrases:
                For each data point d in data:
                    annotation = annotate(d, paraphrase)
                    annotated.append({id, text, annotation, prompt_id, 
                                      prompt: paraphrase, original, 
                                      temperature: temp})
        annotated_data = convert_to_dataframe(annotated)
        all_annotated.append(annotated_data)

        # Calculate KA scores using bootstrapping
        mean_alpha, (ci_lower, ci_upper) = 
            bootstrap_krippendorff(annotated_data, 'prompt_id', bootstrap_samples)
        ka_scores[temp] = {Average Alpha: mean_alpha, CI Lower: ci_lower, 
                           CI Upper: ci_upper}

    # Concatenate all annotated data
    combined_annotated_data = concatenate_dataframes(all_annotated)

    # Save annotated data to CSV if specified
    If save_csv:
        save combined_annotated_data to save_csv

    # Print unique prompts if specified
    If print_prompts:
        Print unique prompts

    # Save prompts for manual editing if specified
    If edit_prompts_path:
        Save prompts to edit_prompts_path

    # Plot KA scores if specified
    If plot:
        Plot KA scores with CI

    Return ka_scores, combined_annotated_data
\end{lstlisting}
